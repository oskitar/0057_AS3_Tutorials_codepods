package codepods.matrix.imagedithering
{

	import flash.display.BitmapData;

	/**
	 * @author oscar de la mata
	 */
	public interface IDitherBitmap
	{

		function dither(bitmapData : BitmapData) : void
	}
}
